import 'package:flutter/material.dart';

class DataFitBox extends StatelessWidget {
  const DataFitBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
